function multiplicar(num1: number, num2: number): number {
    return num1 * num2;
};

function dizOla(nome: string): string {
    return `Olá ${nome}`;
};

console.log(multiplicar(3,8));
console.log(dizOla('Maico'));